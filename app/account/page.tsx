import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function AccountPage() {
  return <PagePlaceholder icon="user" title="Account" phase="Coming in Phase 1 (auth) & Phase 7 — Buyer Profile / Account." />;
}
